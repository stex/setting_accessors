class Create<%= model_name.camelize.pluralize %> < ActiveRecord::Migration[4.2]
  def change
    create_table :<%= model_name.underscore.pluralize %> do |t|
      t.belongs_to :assignable, polymorphic: true

      t.string :name
      t.text   :value

      t.timestamps null: false
    end
  end
end
