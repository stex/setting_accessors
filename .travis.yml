language: ruby
cache: bundler

rvm:
  - 2.0.0
  - 2.1
  - 2.2

before_install: 'gem install bundler'
script: 'bundle exec rake'

notifications:
  email:
    recipients:
      - stex@sterex.de
    on_failure: change
    on_success: never

